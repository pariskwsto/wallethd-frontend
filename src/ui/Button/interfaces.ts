export enum ButtonSize {
  Large = 'large',
  Medium = 'medium',
  Small = 'small',
}

export enum ButtonType {
  Button = 'button',
  Reset = 'reset',
  Submit = 'submit',
}

export enum ButtonVariation {
  Default = 'default',
}
